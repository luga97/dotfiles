<!-- 2026-09-17 05:29:03 [01a0ae73] -->
## Diagnóstico crash aw-server-rust PID 2349846 (2026-09-16 22:34:26) #pc-metrics #diagnostico

- **No era el servicio systemd**: era un HUÉRFANO arrancado a mano desde una sesión pi bajo paseo (cgroup `paseo.service`; aw-server.service arrancó por primera vez 22:40:36, 6 min DESPUÉS del crash). Probable sobreviviente de la verificación manual F1 que la limpieza no mató.
- **Mecanismo (probado por backtrace, ya simbolizado)**: línea de log emitida desde `create_bucket` (worker del datastore) → escritura a stdout FALLÓ (pipe roto de la sesión que lo lanzó) → fern hace panic! si falla la escritura → hook log_panics re-loguea el pánico por el MISMO stdout roto → doble pánico → abort() → SIGABRT.
- **Inferido**: stdout/stderr eran el pipe de la tool-call bash de la sesión (lector muerto → EPIPE); por eso el mensaje del pánico no está en ningún lado. Cliente que creó el bucket a las 22:34:25: no determinable (seguramente prueba manual al arrancar F2).
- **Sin pérdida de datos**: misma sqlite.db, el servicio real la sirvió desde 22:40:36; buckets verificados sanos hoy (eventos frescos). Sin OOM.
- **Lección**: procesos background lanzados con `&` desde sesiones de agente heredan pipes que mueren con la sesión → mueren (o crashean) al escribir. Redirigir a archivo o `/dev/null`, o mejor: no arrancar servicios a mano — usar las unidades systemd. El servicio systemd es inmune (stdout → journald).
- No es bug de Omarchy. Upstream (ActivityWatch) sería como máximo un nit de robustez: fern no debería abortar porque nadie lee su stdout. No se extrae core (no hizo falta gdb); sistema intacto.


<!-- 2026-09-17 06:06:29 [01a0ae9d] -->
## Session Summary (auto, exit: session-end)

### Decisions
- Created the smoke-test task with scope `cwd` instead of `session` after learning the current session is not persisted.

### Lessons Learned
- Session-scoped scheduled tasks require a persisted Pi session; in non-persisted sessions, use scope `cwd` or `global`.
- Scheduler tool (post-patch) works end-to-end: create → list → list → cancel all succeeded.

### Notes
- Task created: `smoke-test` (notify/once, schedule "in 30 minutes", message "prueba parche"), id `task_mu5b1f09_4yi2lj`.
- Both list calls showed the task as enabled/pending with 0 runs; cancellation succeeded.
- Final verification result: PARCHE-OK.

### Follow-ups
- None.

<!-- 2026-09-17 06:15:31 [01a0ae7b] -->
<!-- 2026-09-17 -->
## Sesión: estado + limpieza + case study flotas (desde home-server)

- MEMORY.md tenía marcadores de conflicto git (`<<<<<<<`/`>>>>>>>`) commiteados por el auto-sync (stash pop conflictivo). Resueltos conservando ambas entradas, commit `3070126` en dotfiles.
- **Hallazgo**: `~/.pi/agent/npm` es real por máquina → home-server tenía proper-lockfile SIN parchear → re-aplicado y verificado (PARCHE-OK, exit 0 con 4 calls al scheduler).
- Reproducción aislada del bug con Node (sin bun en home-server): `TypeError: 'get' on proxy: property 'Symbol()' is a read-only and non-configurable data property...` — spec ECMAScript, Node y Bun lo lanzan igual. Issue upstream listo en `/tmp/pi-scheduler-issue.md`, repro verificado palabra por palabra. Luis: publicar después.
- **lugadev F1 iniciado**: elegido case study **telemetría de flotas (Global5)** como primero (cero confidencialidad). Draft v0.1 en `docs/drafts/case-study-fleet-telemetry-es.md` (commit `1e85188`), estructura problema→decisión→trade-offs→resultado, 5 preguntas abiertas para Luis al final (nombre empresa sí/no, técnicas de la reingeniería SQL, tamaño de equipo, detección de fases de descarga, motor de las queries lentas). Orden de case studies acordado: flotas → biométrico → híbrida Avalara.
- PLAN.md actualizado (estado F1 en curso + siguiente acción: responder preguntas del draft).


<!-- 2026-09-17 06:22:12 [01a0ae9f] -->
## Limpieza de contexto: AGENTS.md → skill dotfiles-services (2026-09-17) #infra #decision

- **Motivación**: ~40% de AGENTS.md era dominio-específico (servicios/daemons, paseo, puntero a arch-install) cargado en TODAS las sesiones. Analizado a pedido de Luis; eligió crear skill + limpiar.
- **Skill nueva**: `dotfiles-services` (`~/dotfiles/agents/.agents/skills/dotfiles-services/SKILL.md`, commiteada 2c637b3 y pusheada). Contiene: decisión docker vs nativo, patrón docker (compose, .env gitignored, 127.0.0.1, unless-stopped), patrón systemd user (unidad stoweada, EnvironmentFile, Requires+After, WantedBy=default.target), checklist de alta (stow -R → levantar → verificar → tabla README → commit), ejemplo paseo, enable por-máquina, y advertencia de no arrancar servicios a mano desde sesiones de agente (lección crash aw-server).
- **AGENTS.md**: 43 → 29 líneas, queda 100% comportamiento universal (idioma, estilo, código, memoria, terminal, documentación).
- **Aprendido del setup**: `~/.agents/skills/` tiene symlinks POR-SKILL al repo (el dir contenedor es real). Crear una skill directo ahí deja un dir real sin trackear — el flujo correcto (skill sync-dotfiles): crear/copiar a `agents/.agents/skills/` + `stow -R agents`. En home-server hará falta re-stow (o el timer sync) para que aparezca el symlink de la skill nueva.
- Skill-creator: para skills de referencia con operaciones destructivas reales (stow/systemctl sobre dotfiles), los evals con subagentes no aplican; draft directo + revisión humana.