<!-- 2026-09-19 06:47:08 [01a0b4f3] -->
## Voxtype: Parakeet v3 full precision = GANADOR final del dictado #decision

- **Config final (2026-09-19):** `engine=parakeet`, `model=parakeet-tdt-0.6b-v3` (full, 2.4GB — NO int8), binario ONNX CPU (voxtype-onnx-avx2), streaming=false (v3 no es cache-aware; solo unified-en-0.6b lo es y es inglés-only). Commit 689eb24.
- **A/B mismo párrafo:** v3-int8: 1.36s/29s audio, 3 errores de palabra (huellabas, recesmolido, la frutas) — TODOS por cuantización. **v3 full: 1.78s/34s, CERO errores de palabra** (solo 2 comas). Whisper small GPU: 2.64s/27s, 1-3 errores. Parakeet full = mejor calidad Y mejor velocidad.
- **GPU para Parakeet descartado por análisis:** ruta única = voxtype-onnx-rocm→symlink a migraphx; requiere pila ROCm completa + RX 6650 XT es gfx1032 (NO soportada oficial, requiere HSA_OVERRIDE hack) + int8 parcial en MIGraphX + warmup de compilación. STT ya corre 21× real-time en CPU; el cuello de botella real del flujo es el TIPEO (~1.5-2s/párrafo a 3ms/char).
- **⚠️ BUG/lección infra:** `pkexec voxtype setup ...` (voxtype como root) reescribe config.toml con rename atómico → **ROMPE el symlink de dotfiles** (reemplaza por archivo real). Síntoma: "nothing to commit" tras cambios. Fix: cp vivo→repo, rm, `stow -R omarchy`. Regla: tras cualquier comando voxtype con sudo/pkexec, verificar `readlink ~/.config/voxtype/config.toml`.
- Otros engines ONNX disponibles sin probar: cohere-transcribe-q4f16 (14 idiomas, es/pt incluidos) — único candidato restante si algún día se busca más calidad.
- Pendiente opcional: limpiar modelos obsoletos en ~/.local/share/voxtype/models (base.en 142M, ggml-small 466M, large-v3-turbo 1.6G, parakeet int8 640M ≈ 2.8GB liberables).
