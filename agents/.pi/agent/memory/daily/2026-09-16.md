<!-- 2026-09-16 18:28:00 [01a0ac15] -->
## Extensión /usage de pi: soporte Z.ai

- `~/.pi/agent/extensions/usage.ts` (extensión propia de Luis) ahora soporta el proveedor `zai` además de `openrouter`.
- Endpoint verificado con la clave del Coding Plan: `GET https://api.z.ai/api/monitor/usage/quota/limit` (Bearer, sin spec pública).
- Respuesta: `{code, success, data: {level, limits: [{type, unit, number, percentage, nextResetTime(ms), remaining, usageDetails[]}]}}`.
- Tipos vistos en plan lite: `CREDIT_LIMIT`; `unit` 3=ventana de N horas, 6=semanal (4=días, 5=mensual según oh-my-pi).
- Clave resuelta vía `getProviderAuth("zai")` → `ZAI_API_KEY` → `auth.json`. Plan actual: **lite**.


<!-- 2026-09-16 19:31:36 [01a0ac57] -->
## Session Summary (auto, exit: session-end)

### Decisions
- None.

### Lessons Learned
- Custom models/providers in pi are configured via `~/.pi/agent/models.json` (supports Ollama, vLLM, LM Studio, proxies); local keyless servers need a placeholder `apiKey` and possibly `compat.supportsDeveloperRole` / `compat.supportsReasoningEffort` set to `false`.
- Only models listed in `enabledModels` (settings.json) appear in `/model`, even though the full catalog is available via `pi --list-models`.

### Notes
- Current default model: `zai/glm-5.3-flash` (thinking: medium), provider `zai`.
- Enabled models span zai (glm-5.3, glm-5.3-flash) and openrouter (deepseek, kimi, grok, qwen3-coder-next, gpt-5.6-luna*).
- Presets: `plan`/`review` → moonshotai/kimi-k2.6 (high, read-only); `implement` → kimi-k2.6 (medium, edit); `quick` → z-ai/glm-5.3-flash; `deep` → deepseek/deepseek-v4-pro (high).
- zai provider models range from 200K to 1M context; only glm-5.3-flash supports images.

### Follow-ups
- User may want to change the default model, add/remove enabled models, or add a new provider (Ollama, proxy, etc.) — awaiting their response.