<!-- 2026-09-18 07:27:17 [01a0b3fc] -->
## mail-watch: silenciado de avisos LinkedIn (David A. / Camilo) #infra #decision

- Pedido: los avisos "David A. just messaged you" / "Camilo just messaged you" (amigos que pasan vacantes) no deben llegar a Telegram; al llegar, marcarlos como leídos y archivarlos. Todo lo demás igual.
- Implementado en `~/Projetos/CV/tools/mail-watch.py` (regla 6): si remitente = `messaging-digest-noreply@linkedin.com` y asunto empieza con un nombre de `CVMAIL_SILENCE_LI_NAMES` (sep '|') + " just " → `flag add -f seen` + `message move --to [Gmail]/Todos` (Gmail: archivar = salir del Inbox), sin notificar. Nombres en `tools/.env` (gitignored), con comillas para que sea sourceable Y válido para systemd EnvironmentFile. Commit be8f490.
- Probado: comandos reales sobre 5 avisos viejos del backlog (archivados, IMAP verificado: 0 unread), corrida completa del watcher exit 0, y smoke test con mocks (David A./Camilo silencian; recruiter/Camila/ATS notifican igual que antes).
- Lecciones: himalaya `message move` usa `-f/--from` (no `-m`); IDs de envelope son UIDs estables; UIDs de Gmail son POR-BUZÓN (el mensaje cambia de UID al moverlo); el `envelope list` de himalaya es intermitente (a veces omite mensajes recientes — IMAP raw es la verdad; el timer de 30 min lo tolera).
- Vacante interview: `applications/ancient-graphql/` → entrevista con cliente final **Indeed**, salario 4500 (moneda por confirmar), consultora "Ancient" por confirmar, fecha pendiente. notes.md actualizado. jd.md sigue vacío — pedir JD original. Preparación GraphQL pendiente de respuestas (fecha, formato, JD).


<!-- 2026-09-18 07:48:56 [01a0b420] -->
## Session Summary (auto, exit: session-end)

### Decisions
- None.

### Lessons Learned
- `PI_*` environment variables in this environment only expose `PI_CODING_AGENT=true`; no model/session info is available that way.
- `pi --list-models` returns 398 entries (zai + openrouter), under the 2000-line output display limit.
- Models with `~` prefix are `-latest` aliases; `:batch` variants are cheaper deferred processing; `:free` are free with limits.
- Custom providers/models are configured via `~/.pi/agent/models.json` (not currently present on this machine); the `/model` selector is controlled by `enabledModels` in `~/.pi/agent/settings.json`.

### Notes
- Current default model: `zai/glm-5.3-flash` (1M context, 131.1K max output, thinking, no images via native zai), thinking level `medium`.
- 10 enabled models in `/model`: glm-5.3-flash, glm-5.3, deepseek-pro/flash-latest, deepseek-v4-pro, kimi-k3, kimi-k2.6, grok-4.6, qwen3-coder-next, gpt-5.6-luna variants.
- zai is the only authenticated provider (Coding Plan credential in `auth.json`); openrouter models need `/login` with an API key before use.
- Notable openrouter models: grok-4.20 (2M ctx, 1.8M out), gpt-6-astra (1.1M), claude-opus-5 (1M), glm-5.3 via openrouter (943K out, larger than native zai).

### Follow-ups
- User to decide whether to enable additional openrouter models in `enabledModels` or change the default model (offer pending from assistant).

<!-- 2026-09-18 08:14:42 [01a0b3fc] -->
## Rialto Markets: aclaración de vacantes y prep de entrevista #proyecto #decision

- **Certificado 2026-09-18:** "Rialto Markets" (entrevista 24/09/2026 3–4pm EST, técnica verbal) es una EMPRESA (broker-dealer FINRA, ATS SEC, valores privados) — no una persona. Su JD (fullstack TS/Node/React/Next.js/PG) coincide con su negocio. Carpeta: `applications/rialtomarkets-senior-fullstack-engineer/` con jd.md, notes.md e interview-prep.md (5 partes: empresa/dominio, banco de preguntas ⭐, posicionamiento + crash course Next.js, plan 5 días, preguntas al entrevistador).
- **ancient-graphql es OTRA posición** ("con Ancient", GraphQL/DGS, posible cliente Indeed y 4500 — atribución dudosa, notas marcadas "por confirmar"). La asociación inicial GraphQL↔Rialto fue confusión de Luis, aclarada en chat.
- **Sin evidencia aún:** "el cliente es Indeed" (solo alertas de empleo de Indeed en su correo) y "salario 4500" (moneda/periodo, y a qué vacante corresponde). Pendiente confirmación de Luis en notes.md.
- Hallazgo técnico útil: los portales `app.` e `invest.rialtomarkets.com` corren **Next.js tras CloudFront**; `portal.` aún en KoreConX — tema build-vs-buy probable en la entrevista.
- **Fix infra:** subagentes de pi fallaban con "Model claude-sonnet-4-5 ambiguous across providers" (auth solo tiene openrouter+zai). Corregido: `~/.pi/agent/agents/*.md` → `model: zai/glm-5.2` (scout: `zai/glm-5.3-flash`). Ese dir es LOCAL por máquina (NO está en dotfiles) — home-server tiene sus propios agent files si allá se usan subagentes.
- Pendiente: propuesta de skill/comando `/prep-interview` presentada a Luis (aún sin confirmar).


<!-- 2026-09-18 08:36:44 [01a0b3fc] -->
## Rialto: mapa de vacantes aclarado + Next.js es experiencia real #proyecto #decision

- **Mapa final (2026-09-18):** Ancient = AGENCIA (Luis ve varias vagas con ellos). Miriam = reclutadora de Ancient. Vacante activa: **Rialto Markets** (cliente final), salario **4500 confirmado** (moneda por confirmar), entrevista 24/09 3–4pm EST técnica verbal. El proceso SEO (Google AI bajó tráfico) era de **INDEED** — movido a nueva carpeta de contexto `applications/indeed/notes.md` (sin aplicación activa). `ancient-graphql` sigue siendo otra vaga de Ancient.
- **Next.js: Luis TRABAJÓ bastante con él** ("lo tengo, trabajé bastante, necesitaría repasar conceptos y cosas nuevas"). Contradice el framing "gap" inicial — corregido en interview-prep.md (repaso/puesta al día, no desde cero). Registrado en contributions-log. PENDIENTE preguntar: empresa/periodo, Pages vs App Router, qué construyó → consolidar en experience/ y evaluar si el CV de Rialto debe mostrarlo (JD pide Strong React/Next.js).
- **Regla nueva en AGENTS.md:** antes de tratar una tecnología del JD como gap o afirmar profundidad, SIEMPRE preguntar a Luis (docs pueden estar desactualizados; y al revés tampoco inventar).
- **Skill creada:** `/prep-interview` en `~/Projetos/CV/.pi/prompts/prep-interview.md` — flujo: verificar carpeta → preguntar experiencia contra JD → confirmar enfoque → 3 subagentes (empresa/preguntas/posicionamiento) → interview-prep.md → ofrecer recordatorio y /adapt-cv. Regla central: preguntar siempre.
- Lección de proceso: la asociación vacante↔carpeta se enredó 3 veces por chat; resolver SIEMPRE contra el correo (himalaya search con DSL `body "..."`/`from "..."`) y certificar empresas con ketch antes de crear carpetas.


<!-- 2026-09-18 11:35:27 [01a0b3fc] -->
## Experiencia Lotería del Zulia @ Bclouder (Next.js real) #experiencia

- Luis (2026-09-18): en paralelo con los productos de Bclouder, ~2 años en una **plataforma de loterías para la Lotería del Zulia**: Next.js frontend + NestJS backend. Fue **prácticamente el líder de desarrollo**: ambas partes del código + arquitectura de la solución. Conoce **Pages Router y App Router** (repaso de novedades). Configuró Next.js **desde cero**: ESLint, Prettier, commit hooks, bootstrapping completo.
- Consolidado en `experience/luis_garcia_experience_context_es.md` § Bclouder (marcado pendiente de ampliar). Scratchpad tiene el pendiente: fechas exactas (empleo Bclouder = Ago 2021–Dic 2022, ojo con las "como 2 años" en paralelo), alcance, equipo, métricas, naming del cliente en CV (dominio loterías — decidir si se nombra o se anonimiza).
- interview-prep.md de Rialto actualizado: posicionamiento fuerte ("two years leading development of a Next.js + NestJS platform"), plan de 5 días solo repaso de novedades. Candidato fuerte para el CV de Rialto (JD: "Strong TypeScript, Node.js, React, and Next.js").
- Cuando Luis dé más detalle: usar /add-experience o preguntar directamente; NO dejar el proyecto fuera del CV por desactualización de docs.


<!-- 2026-09-18 12:14:52 [01a0b4f3] -->
## Dictado de Omarchy (Voxtype) diagnosticado y optimizado #infra #decision

- **Herramienta:** Voxtype (dictado de Omarchy, F9 push-to-talk / Super+Ctrl+X toggle), engine whisper.cpp. Config: `~/.config/voxtype/config.toml` (daemon systemd user `voxtype.service`, cambios "needs restart"). GPU AMD RX 6650 XT vía **Vulkan** (RADV) — `voxtype info accel` para verificar.
- **Cambio 2026-09-18:** model `base.en`→`large-v3-turbo` (18/ago, por español)→**`small`** (hoy), `language=es` fijo, `type_delay_ms=3` (editado directo en archivo — el CLI `voxtype config set` NO expone esa clave). Rollback: `voxtype setup model --set large-v3-turbo --restart`.
- **Lecciones de performance:** `language=auto` cuesta ~6s extra por dictado (pasada completa de detección); Whisper siempre codifica ventana fija de 30s (hables 3 o 29s, mismo costo); small = 2.6s por dictado vs 12.6s (large-v3-turbo + auto) vs 0.75s (base.en CPU, inglés-only). Cortes misteriosos con F9 = key-up temprano, no el `max_duration_secs=60` (ese solo corta a los 60s) ni el modelo — toggle evita.
- **Método de análisis:** journalctl `--user -u voxtype -o short-precise`, fases por timestamps: stopped→Transcribing→auto-detected→completed. Logs conservan historia desde jun/2026.
- **Pendientes si Luis pide:** bindings multi-idioma (F9 es / Alt+F9 en / Alt+Shift+F9 pt — requiere script `config set`+restart+`record start` porque `record` no acepta idioma), subir `max_duration_secs` (60), probar `small.en`/quality comparativas.


<!-- 2026-09-18 12:33:29 [01a0b4f3] -->
## Voxtype: benchmark CPU vs GPU + datos finales del tuning #infra

- **Resultado A/B con voz real (mismo párrafo, small, es):** GPU Vulkan 27.3s audio → **2.64s**; CPU AVX2 32.3s → **6.77s** (~3.4s/ventana). **GPU gana, se quedó en GPU.** Benchmark one-shot con ruido (`voxtype transcribe` + anoisesrc): GPU gana en 5s/30s, CPU ganó solo en 65s (anomalía: ruido→tokens alucinados→decoder pesado; Whisper alucina "¡Suscríbete!" con ruido blanco).
- **No existe base.es/.pt**: solo variantes .en (inglés). Para español solo multilingües; small YA es la opción pequeña.
- **Recomendador por hardware:** `voxtype info variants` → sección "Recommended for this hardware" (para omarchy: ★ Whisper Vulkan). Es lo que Luis recordaba.
- **Cambio de variantes GPU↔CPU:** `pkexec voxtype setup gpu --enable/--disable` + `systemctl --user restart voxtype` (reescribe symlink /usr/bin/voxtype, por eso privilegio). Binarios por variante en /usr/lib/voxtype/voxtype-{vulkan,avx2} — llamables directo para benchmarks sin tocar el daemon.
- **Config final 2026-09-18:** small + es + GPU + type_delay_ms 3 + max_duration_secs 180 → dictados ~2.6s (venían en 12.6s). Pendientes no solicitados: multi-idioma por binding, small.en si dicta inglés.


<!-- 2026-09-18 12:50:19 [01a0b4f3] -->
## Voxtype: decisión final — multilingüe con auto #decision #infra

- **Config definitiva (elección de Luis 2026-09-18):** `small` + `language=auto` + GPU Vulkan + `type_delay_ms=3` + `max_duration_secs=180`. Dictados: ~2s (frases cortas), ~4.3s (párrafos ~27s). Luis valoró que los ~2s extra de la detección valen la pena vs `es` fijo (2.4s) para trabajar trilingüe.
- **Costo de auto con small medido:** +1.9s (~4.32s vs ~2.4s fijando es) para ~24s de audio.
- **Code-switching (probado con párrafo trilingüe):** Whisper elige UN idioma por ventana de 30s (detector da confianza menor, 0.845). Préstamos técnicos EN sobreviven (deployment, merge, release, downtime); palabras función y frases PT se españolizan — "because"→"porque", "sim"→"sin" (cambio de significado). Portugués puro: detecta `pt` (p=0.978) y transcribe perfecto. Regla práctica: un idioma por dictada.
- **BUG upstream pendiente:** `whisper.language="es,en,pt"` (lista de comas, documentada en el schema) produce basura total ("clap clap clap", detecta polaco "prawa") y tiempos 16-25s — feature rota en voxtype 1.0.1-1. Revertida a auto puro. Candidato a issue en voxtype.io.
- Inglés puro: sin probar en dictado real pero auto lo cubre (misma mecánica que pt).


<!-- 2026-09-18 13:19:04 [01a0b4f3] -->
## Paseo: dictado por voz habilitado en ambos daemons + skill de diccionario #infra #decision

- **Arquitectura aclarada:** paseo = daemon POR máquina; el STT corre EN el CPU del daemon dueño de la sesión (el micrófono está en el cliente, el audio se streamea al daemon). No es motor compartido: cada daemon necesita features+modelo propios (~600MB en ~/.paseo/models/local-speech).
- **Config aplicada 2026-09-18 (ambas máquinas):** `features.dictation = {enabled: true, stt: {provider: "local", model: "parakeet-tdt-0.6b-v3-int8", language: "es"}}`. v3 = 25 idiomas europeos con autodetección (es/en/pt). home-server ya tenía v2-inglés+kokoro+silero descargados de ago/2025; v3 se descarga al primer uso. voiceMode queda off (TTS kokoro es inglés-only).
- **`paseo reload` NO aplica cambios de dictado** — valida la config pero exige `paseo daemon restart` para features.dictation.*. Restart mata los pi RPC hijos; los idle retoman desde disco (JSONL), solo arriesga el turno en curso.
- **Skill nueva `dictation-dictionary`** (dotfiles agents/.agents/skills/, commit aedd872): flujo para añadir palabras al diccionario de voxtype `[text] replacements` — buscar garabatos reales en journalctl de transcripciones, mapearlos, restart, commit. Paseo NO tiene diccionario custom (sin replacements/hotwords en su STT — candidato a FR upstream).
- **Diccionario inicial voxtype:** marchi/omarchee→omarchy, boxtype/boxdade→voxtype, pksec→pkexec, wishbear→whisper (garabatos reales del historial, encontrados con grep fonético).
- Agentes de paseo en omarchy al momento: 7 (6 idle + 1 running = la sesión del agente). `paseo ls` para listar.
