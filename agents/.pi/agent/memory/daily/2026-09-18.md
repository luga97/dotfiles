<!-- 2026-09-18 07:27:17 [01a0b3fc] -->
## mail-watch: silenciado de avisos LinkedIn (David A. / Camilo) #infra #decision

- Pedido: los avisos "David A. just messaged you" / "Camilo just messaged you" (amigos que pasan vacantes) no deben llegar a Telegram; al llegar, marcarlos como leídos y archivarlos. Todo lo demás igual.
- Implementado en `~/Projetos/CV/tools/mail-watch.py` (regla 6): si remitente = `messaging-digest-noreply@linkedin.com` y asunto empieza con un nombre de `CVMAIL_SILENCE_LI_NAMES` (sep '|') + " just " → `flag add -f seen` + `message move --to [Gmail]/Todos` (Gmail: archivar = salir del Inbox), sin notificar. Nombres en `tools/.env` (gitignored), con comillas para que sea sourceable Y válido para systemd EnvironmentFile. Commit be8f490.
- Probado: comandos reales sobre 5 avisos viejos del backlog (archivados, IMAP verificado: 0 unread), corrida completa del watcher exit 0, y smoke test con mocks (David A./Camilo silencian; recruiter/Camila/ATS notifican igual que antes).
- Lecciones: himalaya `message move` usa `-f/--from` (no `-m`); IDs de envelope son UIDs estables; UIDs de Gmail son POR-BUZÓN (el mensaje cambia de UID al moverlo); el `envelope list` de himalaya es intermitente (a veces omite mensajes recientes — IMAP raw es la verdad; el timer de 30 min lo tolera).
- Vacante interview: `applications/ancient-graphql/` → entrevista con cliente final **Indeed**, salario 4500 (moneda por confirmar), consultora "Ancient" por confirmar, fecha pendiente. notes.md actualizado. jd.md sigue vacío — pedir JD original. Preparación GraphQL pendiente de respuestas (fecha, formato, JD).
